import { ShorAmountPipe } from './shor-amount.pipe';

describe('ShorAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new ShorAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
